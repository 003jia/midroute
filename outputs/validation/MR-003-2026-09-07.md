# MR-003 验收记录 · 2026-09-07（部分完成）

- 任务：MR-003 领域合约与数据库增量迁移（状态：部分实现）
- 快照：**未提交工作区**（Git 零提交），HEAD 不存在，不虚构 SHA。

## 变更文件
- `apps/server/internal/domain/domain.go`：新增 AccountMode/AuthType/BillingMode/AuthState/CapabilityStatus 枚举；Account 增 mode/auth_type/billing_mode/auth_state 字段。
- `apps/server/internal/db/db.go`：migration v2（accounts 四列 + `UNIQUE(provider_id, upstream_id)`）；`Migrate` 增加未来 schema 前向拒绝；`KnownVersion()`。
- `apps/server/internal/repository/repository.go`：账户读写覆盖新列；新增 `SetAccountAuthState`、`UpdateAccountSecretRef`。
- `apps/server/internal/api/api.go`：createAccount 设置枚举默认值，接受 `mode` 字段（枚举校验）。
- `docs/contracts/data-contracts-v1.md`：新增版本化合约（枚举/空值/单位/来源语义）。
- `apps/server/internal/db/db_test.go`：新增 v1→v2 升级保留数据、前向拒绝、唯一索引三个测试。

## 验证
- 命令：`./build/test.sh`（根目录）→ Go 测试/type-check/lint/secret-scan 全部通过。
- 单项：`go test ./internal/db/ -count=1` ok；`go test ./internal/api/ -count=1` ok。

## 验收框勾选
- [x] 第 1 条（同名模型不覆盖 + 账户模式/计费/能力可表达）——证据如上。
- 第 2 条缺"迁移失败注入的回滚测试"；第 3 条依赖 MR-028，均未勾。

## 未解决问题
- 迁移失败事务回滚无失败注入测试（本条验收框第 2 项）。
- `docs/contracts/` 仅覆盖数据语义；错误码合约仍在 `internal/errs`，未版本化成文档。
