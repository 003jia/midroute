# MR-006 / MR-007 / MR-008 验收记录 · 2026-09-07（M3 开发轮）

- 任务：MR-006 首批平台能力与源码验证、MR-007 OAuth 登录/刷新/撤销、MR-008 订阅与额度读取适配器（均为部分实现）
- 快照：**未提交工作区**（Git 零提交），HEAD 不存在，不虚构 SHA。

## MR-006
变更文件：
- 新增 `docs/provider-capabilities.md`：7 类平台矩阵；Codex/ChatGPT 全套证据（OAuth 端点/client_id/PKCE/scope/刷新单飞/转发端点/被动额度头/流内帧），全部标注证据文件:行号与冻结提交 `17a65ee5470fbaf0e22fc219381e6a4ae9e07624`。
- 更新 `docs/source-ledger.md`：登记本轮参考移植 + 修复 ISS-18（OneAPI 短 SHA → 全 SHA）。
验证：人工核对上游源码路径（work/CLIProxyAPI），矩阵中每个结论附文件:行号。
勾选：第 1 条 [x]（矩阵 7 类齐全、unknown 明确）。第 2 条缺"兼容版本"实测记录；第 3 条待真实凭据统一确认。
边界结论（重要）：Codex **无公开主动余额查询端点证据**，额度采集定位为被动观测；撤销无官方端点证据，仅本地断开。

## MR-007
变更文件：
- 新增 `apps/server/internal/connectors/oauth/`：`oauth.go`（Provider 描述/Codex 参数/TokenBundle/错误集）、`pkce.go`（S256）、`state.go`（state 随机/10 分钟 TTL/单次消费）、`service.go`（StartAuth/CompleteAuth/Refresh 单飞/Revoke）、`callback.go`（本机回调监听/手动粘贴解析）。
- `apps/server/internal/credentials/`：`Vault` 接口新增 `Persistent()`；Keychain=true，内存=false。
- 测试 `oauth_test.go`：9 个用例 + race。
验证：`go test ./internal/connectors/oauth/ -race -count=1` ok（含 10 并发单飞仅 1 次上游请求、刷新失败保留旧凭据、ErrRefreshRejected 判定、内存 Vault 拒绝发起授权、回调超时）。
勾选：第 1 条 [x]（mock 层逐项覆盖）、第 3 条 [x]（token 不进 URL/日志/业务表 + 持久化守卫）。第 2 条"失效进入 reauth_required"未接线，未勾。

## MR-008
变更文件：
- 新增 `apps/server/internal/quota/`：`quota.go`（Snapshot/Observation，nil=未知语义）、`codex.go`（x-codex-* 响应头解析：primary/secondary/additional-<名> 命名空间，used-percent/window-minutes/reset-after-seconds/reset-at/allowed/limit-reached，非法值跳过并记 warning）。
- 测试 `codex_test.go`：6 个 fixture 用例。
验证：`go test ./internal/quota/ -count=1` ok。
边界：SSE 流内 `codex.rate_limits` 帧解析随 MR-013；快照持久化随 MR-009；primary/secondary 窗口语义（5h/周）为未验证推断，以实测 `-window-minutes` 为准。
勾选：全部未勾（fixture 覆盖部分达成；真实账户验证与持久化未做）。

## 全量验证
- `./build/test.sh`：Go 测试（含新增 oauth/quota 包）、前端 type-check/lint、secret-scan 全部通过。

## 遗留（阻塞真实验收的事项）
1. Codex 真实账户浏览器授权（本地 `security` CLI Keychain 环境）。
2. 真实响应头字段与窗口数值回填矩阵。
3. OAuth 管理 API 端点接线（依赖 MR-002 会话/CSRF）。
4. `reauth_required` 状态接线到刷新失败路径（依赖账户仓储接线）。
