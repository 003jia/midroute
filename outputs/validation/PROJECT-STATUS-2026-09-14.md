# Midroute 当前项目状态对齐

> 历史快照：当前状态已由 [2026-09-18 核对记录](./PROJECT-STATUS-2026-09-18.md) 替代。下文的 41/96 和完成度估算保留供追溯；本轮拆分复合条目并纠正过度勾选后不再沿用此统计口径。

日期：2026-09-14  
代码快照：`25123ff`  
范围：以当前工作区代码、实施清单、验证文件和实际构建结果为准；不把历史方案、mock 成功或页面文件存在视为真实平台验收。

## 总体结论

Midroute 已从工程骨架进入后端主体和部分管理页面可用阶段。核心账户、凭据、安全、基础中继、额度数据结构、请求记录和费用聚合已有实现及自动化测试；真实 Coding Plan、完整拥挤度路由、全管理台、恢复导入和发布交付仍未完成。

实施清单共有 32 个 MR、96 个验收框：41 个已勾选，55 个未勾选。按验收框计算为 42.7%，但这不是发布完成度；真实 Provider、浏览器、恢复和安装包验收尚未完成，因此当前可交付产品完成度按保守口径约为 35%–45%。

## 已完成并有自动化证据的能力

| 范围 | 当前能力 | 主要证据 |
|---|---|---|
| 工程与安全 | 单服务构建、静态页托管、会话、CSRF、Host/DNS rebinding 防护、密钥扫描、隔离冒烟 | MR-001、MR-002 |
| 账户管理 | Provider/账户生命周期、凭据轮换、monitor_only、能力矩阵、账户和模型页面代码 | MR-004、MR-005、MR-006 |
| OAuth 与额度底座 | OAuth 管理 API、PKCE、SecretRef 旋转、被动额度解析、额度池/快照/手工补充、刷新任务 | MR-007、MR-009、MR-010、MR-011 |
| 推理网关 | Chat Completions、Responses 原样透传及粘性句柄、Anthropic Messages/count_tokens、项目令牌与并发控制 | MR-013、MR-014、MR-016 |
| 请求与费用 | 每次尝试幂等记录、结构化错误、定点价格版本、按事件时价格聚合、usage summary API | MR-012、MR-015、MR-019 |

## 仍是交付阻塞项

1. 真实 Coding Plan/OAuth 账户、套餐和 5 小时/周额度窗口验证。
2. 首个真实 Coding 客户端闭环：接入账户、转发、工具调用、流式取消、请求详情与用量复核。
3. OpenAI 流式 usage、reasoning 和 cache read/write 的完整采集。
4. 健康样本、TTFT、主动探测、熔断、权重路由、提醒和实时健康页面。
5. 预算预留、订阅耗尽后的显式付费回退控制、用量/预算/对账页面。
6. 脱敏审计、恢复流程、OneAPI dry-run 导入、许可证门禁、macOS 安装包、Docker 和完整发布回归。

## 本次复核命令

| 命令 | 结果 |
|---|---|
| `./build/test.sh` | 通过：Go 测试、type-check、lint、secret scan |
| `./build/build.sh` | 通过：后端、前端及运维工具构建 |
| `./build/smoke-test.sh` | 通过：healthz、readyz、空库初始化、管理页、JSON 404、伪造 Host 403 |

说明：受限沙箱不能绑定 `httptest` 的 `[::1]:0` 回环端口；在允许本机回环的环境重跑后，测试通过。这是执行环境限制，不是测试断言失败。

## 文档同步规则

- README 只描述当前产品里程碑，不引用历史方案的完成结论。
- 实施清单的状态、验收框和 `outputs/validation/` 必须同步；代码完成但缺浏览器或真实 Provider 验收时标为“部分实现”或“待真实验收”。
- 代码问题清单的状态表以当前状态为准，问题小节中的旧行号只保留历史核查依据。
- 缺真实凭据时，继续实现不依赖凭据的代码与 fixture，但不得勾选真实验收项。
