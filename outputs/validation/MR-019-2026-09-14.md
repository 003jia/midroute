# MR-019 Token 聚合、价格版本与官方对账 验收证据（P1-2）

> 日期：2026-09-14
> 范围：MR-019 用量/费用聚合与价格版本

## 一、实现内容

- migration v9：`price_versions` 增加定点列 `input/output/cache_read/cache_write_price_nano`（nano-USD/令牌 int64，不用 float 累加）；索引 `(model_id, effective_at)`。
- domain：`PriceVersion`（nano 价格）、`UsageRow`（聚合行，CostNanoUSD int64）。
- repository：
  - `UpsertPriceVersion` / `ListPriceVersions` / `PriceAt(model, t)`（版本边界：取 <=t 最新）。
  - `AggregateUsage`：按维度分组，费用用**事件时价格版本**相关子查询结算；cache read/write、reasoning 分列不重复叠加。
- API：
  - `GET/POST /api/v1/price-versions`。
  - `GET /api/v1/usage/summary`（provider/account/model/project 过滤；scope=observed_by_midroute；返回 cost_estimable 提示）。

## 二、执行命令与结果

| 命令 | 结果 |
|---|---|
| `go build ./...` / `go vet ./...` | 通过 |
| `go test ./...` | 全部通过（新增 TestPriceAtVersionBoundary、TestPriceAtAndAggregateCost、TestPriceVersionsAndUsageSummary） |
| `./build/test.sh` | 通过 |

关键断言：
- 跨价格版本边界（8 月 p1、9 月 p2）聚合成本 = 10,000,000 nano（各按事件时版本）。
- PriceAt 取 <=t 最新版本；首个版本之前返回 missing。
- API 汇总金额与手算一致；Source=observed。

## 三、数据库 migration

- 最高 schema 版本：**9**（v9=price_versions 定点化）。

## 四、真实凭据与浏览器验证

- 无需真实凭据（纯本地聚合逻辑；官方对账适配器沿用 mock 层）。
- 前端 Usage 页面未建（MR-027 范围）。

## 五、遗留

- 官方账单/本地观测对账页（MR-027）。
- 预算预留/结算（MR-020，下一步）。
- 金额展示与 UI（MR-027）。