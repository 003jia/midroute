# MR-014 Anthropic Messages 协议 验收证据（P1-1）

> 日期：2026-09-14
> 范围：MR-014 Anthropic Messages 与计数

## 一、实现内容

- `internal/api/anthropic.go`：
  - `POST /v1/messages`：原生 Anthropic Messages 透传。仅改写 `model` 字段（rewriteAnthropicModel），其余字段原样保留（不静默删字段）；按 allowlist 保留 `anthropic-version`/`anthropic-beta`/`content-type`，`x-api-key` 由目标账户注入。
  - `POST /v1/messages/count_tokens`：转发上游原生端点；无账户返回明确错误；不落 tokenizer 估算。
  - 流式：SSE 事件按空行切分原样透传（text_delta/input_json_delta/tool 事件/缓存字段均保留）。
  - 非流式响应提取 usage 落用量（recordAnthropicUsage），不解析正文。
  - 复用 `Router.ResolveCandidates` 选路；失败按 `connectors.Retryable` 切换。
- 路由挂载：`/v1/messages`、`/v1/messages/count_tokens`（requireToken，MR-016 令牌鉴权）。

## 二、执行命令与结果

| 命令 | 结果 |
|---|---|
| `go build ./...` / `go vet ./...` | 通过 |
| `go test ./...` | 全部通过（新增 TestAnthropicMessagesPassthrough、TestAnthropicMessagesStreamingPassthrough） |
| `./build/test.sh` | 通过 |

测试断言：
- 非流式：透传响应、模型改写为目标模型且正文保留、anthropic-version/beta 头透传、usage 提取。
- 流式：input_json_delta/tool 事件不被丢弃。
- count_tokens：透传上游计数响应。

## 三、数据库 migration

- 无新增 migration（最高版本 8）。

## 四、真实凭据与浏览器验证

- Claude Code 真实工具往返：**待真实凭据**（保持未勾）。
- 无界面变更。

## 五、遗留

- 兼容矩阵（MR-006 文档）补充 Claude Code 实测版本。
- Anthropic 入站协议跨协议转换（OpenAI→Anthropic 已存在；本任务为原生透传优先）。