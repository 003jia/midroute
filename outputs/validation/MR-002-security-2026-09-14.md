# MR-002 安全边界收口验收记录 · 2026-09-14

- 任务：MR-002 第 2 框（会话/Host/CSRF）+ ISS-04/06/08 脚本安全组
- 快照：基于 0ec517c 的未提交工作区（本轮提交后以新提交号为准，见 git log）。

## 变更文件
- `apps/server/internal/session/session.go`：重写。新增——免登录路径强制 loopback 字面量 Host（DNS rebinding 防护）；写请求 Origin/Referer 同源校验（CSRF）；会话存储（随机令牌、12h 滑动 TTL、进程内存不持久化）；`IssueSession/RevokeRequestSession/LocalOnlyFree`；不读取任何 X-Forwarded-* 头。
- `apps/server/internal/httpserver/httpserver.go`：挂载 `/api/v1/session`（POST 登录签发 HttpOnly/SameSite=Strict Cookie；DELETE 退出注销并清 Cookie；本地免登录模式 400）。
- `apps/server/internal/session/session_test.go`：新增 4 个用例（Host 字面量、写同源、会话生命周期与过期、并发 race）。
- `apps/server/internal/httpserver/httpserver_test.go`：新增 4 个用例（伪造 Host 403、跨站写 403/同源放行、会话登录-访问-退出-失效全流程、本地模式拒发会话）；存量用例补合法 Host。
- `build/ci/secret-scan.sh`（ISS-06）：命中输出改为 `文件:行号 (疑似密钥，内容已隐藏)`；排除 `.mimosa/`。
- `build/smoke-test.sh`（ISS-08）：mktemp 唯一数据目录；端口 20000-40000 随机探测空闲；每轮探测前校验本次进程存活（防误探他人实例）；trap 精确清理；新增伪造 Host 403 断言。
- `build/backup-data.sh`（ISS-04）：`midroute.db` sqlite3 在线备份（缺 sqlite3 时拒绝复制运行中 WAL 库并退出）；默认排除 `local.keys`/`data.key`（`--include-secrets` 显式携带并 chmod 600）；manifest 记录 app_version（VERSION 文件）、schema_version（schema_migrations 表）、includes_secrets、逐文件 SHA-256；清理备份边车 `-wal/-shm`。
- `.gitignore`：补 `/.mimosa/`。

## 验证
- `go test ./internal/session/ ./internal/httpserver/ ./internal/api/ -race -count=1` 全部 ok。
- `./build/test.sh` 全量通过（Go 测试 + go vet + type-check + lint + secret-scan）。
- `./build/smoke-test.sh` 实跑通过：随机端口（本轮 24281）、mktemp 数据目录、页面 HTML 断言、未知 API JSON 404、**伪造 Host → 403**。
- secret-scan planted-key 自测：植入假密钥 `scan-test-fake.go` 后输出 `scan-test-fake.go:6 (疑似密钥，内容已隐藏)`、exit 1，无内容回显；删除后恢复 exit 0。
- backup-data.sh 实跑：默认包无 local.keys/data.key；`--include-secrets` 时包含；manifest 含 `app_version: 0.1.0-dev` 与逐文件 sha256。

## 验收框
- MR-002 第 2 框 `[x]`：远程无认证 401（TestProtectedRouteRequiresAuthWhenRemote / TestSessionLoginLogout）；伪造 Host 403（session + httpserver 双层测试 + 冒烟实跑）；跨站 Origin/Referer 写 403、同源与无 Origin 客户端放行（TestCrossSiteWriteRejected）；退出后会话失效（TestSessionLoginLogout 末段）；本地免登录不重复输入（loopback free path 既有测试）。MR-002 三框全勾，状态改已验收。
- ISS-04/06/07/08 状态更新为已修复（恢复入口/损坏包测试仍属 MR-028 后续，未宣称）。

## 遗留
- MR-028 恢复入口、备份恢复测试未做（ISS-04 只修备份侧）。
- 会话为进程内存态：重启即全部失效（设计选择，已注释说明）；未来多实例部署需外部会话存储。
- 代理/TLS 部署时 Cookie 未加 Secure（本地 HTTP 部署形态；反代场景文档说明由代理保证传输安全）。
