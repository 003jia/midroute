你现在要继续开发 Midroute 自主中间路由平台。

项目目录：/Users/jiayancheng/Documents/api集中管理平台
代码基线：25123ff；任务整理日期：2026-09-18。

先读：
1. tasks/midroute-implementation-checklist.md：唯一任务状态入口，含 32 项 MR 的完成/待办总览、实现步骤、依赖和规范。
2. tasks/midroute-code-audit-issues.md：当前缺陷及修复边界。
3. outputs/validation/PROJECT-STATUS-2026-09-18.md：最近核对与验证结果。
4. tasks/prd-midroute-independent-platform.md：产品目标；docs/contracts、docs/provider-capabilities.md、docs/source-ledger.md：数据、能力和来源约束。

当前统计：48 个已完成子项、69 个待办，共 117 项。
此口径由原 96 个复合验收框拆分并纠偏而来，不是新增功能数量或发布完成百分比。历史文档不能覆盖当前任务状态。

一、已经完成的部分，直接复用

- 单个 Go 服务、React/TypeScript 前端、SQLite 增量迁移、根目录构建/测试入口。
- 静态页托管、loopback 免重复管理登录、会话/Host/同源写访问边界。
- Provider/账户核心生命周期、Keychain/SecretRef、凭据轮换、仅监测过滤、能力矩阵。
- Accounts、Models、Quota 页面代码；类型检查、lint 和构建通过，浏览器验收仍未完成。
- OAuth 管理 API、PKCE、刷新单飞、失效状态和凭据轮换；真实授权未验收。
- Codex 额度响应头解析 fixture、额度池/成员/历史快照、手工补充。
- 手动刷新执行器、任务去重、失败计数/下次时间记录、重启 interrupted 标记；没有自动执行循环。
- Chat Completions、Responses 原样透传/句柄绑定、原生 Anthropic Messages/count_tokens。
- 项目令牌 API、权限校验、单令牌并发、请求尝试幂等和详情 API。
- 部分管理审计写入、定点价格版本、事件时取价、本地用量汇总。
- 健康评分引擎、默认 data/ 主库在线备份、来源台账、扫描脱敏。

不得重复搭建这些模块，也不得重新引入 OneAPI、CLIProxyAPI、CPA 的运行服务。上游仅为已登记来源的选择性移植参考。

二、按依赖推进剩余工作

批次 A：先把数据修准
对应 MR-008/009/012/015/019，ISS-10/11/15/20/21。
1. 额度百分比、窗口时长、作用范围和重置来源分字段保存；修复 WindowMinutes 写入 percent 快照 Limit。
2. 补齐 Chat SSE usage/final chunk，保留 reasoning/cache read/cache write 含义。
3. 缺 usage、取消、断流、结果未知时写入明确完整度，不能显示为已知 0 Token。
4. 缺价格或仅部分记录有价格时给出不可估算/部分估算状态；处理 SQL NULL，输出真实的可估算字段。
5. 验证 input/cache/reasoning 包含关系、币种边界和价格版本；补时间聚合及同范围的真实账单对账。
6. 保留现有首块后禁止整请求重放、状态句柄原账户绑定、请求尝试幂等。

批次 B：补日常操作
对应 MR-004/005/010/011/015/016/017。
1. 将自动计划、实际重试、暂停、恢复接入服务生命周期；next_run_at 本身不等于调度完成。
2. 补 Retry-After、授权失效暂停、全局并发、休眠补跑和持久化错误处理。
3. 补凭据清理失败重试、审计查询/覆盖/写入失败处理。
4. 实现同项目多个令牌共享并发边界；现有计数只按 tokenID。
5. 增加令牌/客户端配置/请求详情页面；复用已有后端，勿重建 Accounts/Models/Quota。
6. 补额度单位、趋势与倒计时；实际完成桌面和移动浏览器验收。

批次 C：跑通首个真实闭环
对应 MR-006/007/008/013/018；Claude Code 另对应 MR-014。
1. 用已授权真实账户分别验证登录、刷新、模型发现、套餐/窗口读取与推理。
2. 首个工具默认 Codex，完成文本、工具调用/结果、流式取消、有状态续接和重启复查。
3. 对照请求详情与实际用量，记录 Provider、客户端、Connector 版本、日期和脱敏结果。
4. 无权限或平台不支持时明确 unsupported/permission_required/unknown，不把普通 API Key 成功视为 Coding Plan 成功。
5. Claude Code 保持原生 Messages 透传，单独验证真实工具往返和 beta/count_tokens 范围。

批次 D：完成日常监测和费用控制
对应 MR-020–027，ISS-14/16。
1. 预算预留、结算、取消、重复提交、崩溃恢复和显式付费回退。
2. 网关采集 TTFT/总延迟/错误，写 health_samples，提供健康和历史 API。
3. 熔断/半开/恢复、同级权重、共享额度与健康过滤；样本不足不误判拥堵。
4. 主动推理探测需明确开启并有频率/费用预算，消耗单独记录。
5. 提醒去重、冷却、恢复事件；完成路由、用量、预算、对账、健康、设置和通知页面。
6. 管理事件流断线/休眠/切页不重复订阅，缓存不能冒充实时数据。

批次 E：恢复和发布
对应 MR-001/002/003/028–032，ISS-01/04/06/22。
1. 修复显式 SMOKE_DATA 任意目录删除；修复前不要使用该参数。此项可独立提前处理。
2. 生产静态资源缺失处理、嵌套依赖/构建物扫描排除。
3. 备份遵守实际配置数据目录及明确白名单，默认不携带明文凭据。
4. 恢复到新目录，测试损坏包/旧库/未来 schema/凭据缺失，不覆盖运行目录。
5. OneAPI 只读 dry-run 导入、冲突报告、幂等 apply；不修改源库。
6. 来源、许可证和依赖门禁；macOS 安装升级、Docker 凭据持久化、完整发布回归。
7. 普通 API Key、Codex、Claude Code 等兼容范围逐项标支持/部分/不支持/未验证。

三、实现与验收规则

- 按实施清单第 3、5、6 节执行；先处理字段合约与 migration，再处理 repository/service/API/UI。
- Go 使用 gofmt/go vet、errors.Is/As；前端遵守 strict TypeScript、完整空态/加载/错误/过期状态。
- unknown 数值为 null；source、confidence、availability、freshness 分开；5 小时和每周窗口不相加。
- 官方账单与本地观测分开；金额用定点数，缺价格不显示免费。
- SecretRef 持久化；密钥、OAuth Token/code/state/verifier、请求正文不进入普通日志、导出或截图。
- 真实测试只使用已授权账户；缺凭据时继续独立代码/fixture，保留真实验收待办。
- 功能改动执行适当的单测、根目录 test/build/smoke；并发改动补 race，界面改动补桌面 1440×900/移动 390×844。
- 仅整理文档时检查链接、统计和差异即可，不重复跑与改动无关的功能测试。
- outputs/validation/MR-NNN-YYYY-MM-DD.md 留存日期、代码版本、命令/操作、退出码、范围与未验证项。
- 将完成子项标为 [x]，剩余部分拆为独立 [ ]；同步总览、问题清单、README 和本提示词。只有对应 MR 全部要求通过才标整项已验收。

交付时说明：完成的 MR 子项、修改文件、migration 是否变化、测试结果、真实凭据/客户端验收边界、遗留项及证据路径。
